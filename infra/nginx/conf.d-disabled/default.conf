# Intentionally empty. Mounted over the nginx image's own /etc/nginx/conf.d/default.conf, whose
# `server_name localhost` block sorts before the rendered prod.conf and so answered every
# unrecognised request with the stock welcome page, nginx version included.
# The real server blocks are rendered from templates/prod.conf.template.
