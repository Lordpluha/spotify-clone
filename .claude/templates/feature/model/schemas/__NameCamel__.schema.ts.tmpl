/**
 * Placeholder for this feature's own Zod schema (e.g. a multi-step form's
 * transient shape). Never duplicate an entity's schema here — import it from
 * the owning entity instead (see `.claude/rules/forms.md` "Schema
 * co-location rule").
 */
export {}
