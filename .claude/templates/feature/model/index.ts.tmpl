export type { __NamePascal__Props } from '@/__Layer__/__NameKebab__/model/__NameCamel__.types'

// dtos/, schemas/, and responses/ start empty — populate them only when this
// feature introduces a shape not already owned by an entity.
