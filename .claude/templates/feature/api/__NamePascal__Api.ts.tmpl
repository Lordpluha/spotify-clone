'use client'

import { ClientApi } from '@shared/api/client'

/**
 * Client-side API calls owned by this feature, if it talks to an endpoint
 * no entity already wraps. Add typed methods here; prefer a
 * use__NamePascal__* hook over calling this class directly from UI.
 */
class __NamePascal__ApiClass extends ClientApi {}

export const __NamePascal__Api = new __NamePascal__ApiClass()
