'use client'

import { ClientApi } from '@shared/api/client'

/**
 * Client-side __NamePascal__ API calls for use inside Client Components and
 * hooks. Add typed methods here; prefer wrapping calls in a
 * use__NamePascal__* hook (see `.claude/rules/react.md` "Hooks-wrapping
 * convention") rather than calling this class directly from UI.
 */
class __NamePascal__ApiClass extends ClientApi {}

export const __NamePascal__Api = new __NamePascal__ApiClass()
