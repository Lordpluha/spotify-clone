import type { __NamePascal__ } from '@/__Layer__/__NameKebab__/model/schemas/__NameCamel__.schema'

/**
 * Extend this type when an endpoint returns __NamePascal__ joined with extra
 * computed or related fields beyond the core entity shape (e.g. a playlist
 * response that embeds its tracks).
 */
export type __NamePascal__Response = __NamePascal__
