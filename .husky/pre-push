#!/bin/sh

# Проверяем переменную WEB_ONLY
# Если установлена - билдим web-стек (api, web, ui-react)
if [ "$WEB_ONLY" = "true" ]; then
  echo "🌐 Building web stack (api, web, ui-react)..."
  pnpm --filter @spotify/api --filter @spotify/web --filter @spotify/ui-react build
  exit 0
fi

# По умолчанию билдим всю монорепу
echo "🏗️  Building full monorepo..."
pnpm build
